--- a/Source/Core/DiscIO/Volume.cpp
+++ b/Source/Core/DiscIO/Volume.cpp
@@ -3,6 +3,8 @@
 
 #include "DiscIO/Volume.h"
 
+#include <TargetConditionals.h>
+
 #include <cstring>
 #include <functional>
 #include <map>
@@ -142,12 +144,23 @@ std::unique_ptr<VolumeDisc> CreateDiscForCore(const std::string& path)
 {
   auto reader = CreateBlobReader(path);
 
+#if TARGET_OS_IPHONE
+  return TryCreateDisc(reader);
+#else
   if (Config::Get(Config::MAIN_LOAD_GAME_INTO_MEMORY))
     return TryCreateDisc(reader, CreateScrubbingCachedBlobReader);
 
   return TryCreateDisc(reader);
+#endif
 }
 
+#if TARGET_OS_IPHONE
+std::unique_ptr<VolumeWAD> CreateWAD(const std::string& path)
+{
+  (void)path;
+  return nullptr;
+}
+#else
 static std::unique_ptr<VolumeWAD> TryCreateWAD(std::unique_ptr<BlobReader>& reader)
 {
   if (!reader)
@@ -172,6 +185,7 @@ std::unique_ptr<VolumeWAD> CreateWAD(const std::string& path)
 {
   return CreateWAD(CreateBlobReader(path));
 }
+#endif
 
 std::unique_ptr<Volume> CreateVolume(std::unique_ptr<BlobReader> reader)
 {
@@ -179,9 +193,11 @@ std::unique_ptr<Volume> CreateVolume(std::unique_ptr<BlobReader> reader)
   if (disc)
     return disc;
 
+#if !TARGET_OS_IPHONE
   std::unique_ptr<VolumeWAD> wad = TryCreateWAD(reader);
   if (wad)
     return wad;
+#endif
 
   return nullptr;
 }
